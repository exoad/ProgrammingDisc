const {MessageEmbed} = require("discord.js");
const { Database } = require("secure-db");

module.exports = {
  config: {
    name: `ping`,
    category: "",
    description: "",
    aliases: [``],
  },
  run: async (bot, message, args) => {
    try {
      
    } catch (e) {
      console.log(e);
    }
  },
};
